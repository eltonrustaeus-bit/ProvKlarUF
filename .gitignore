node_modules/
*.png
# Undantag: ikonuppsättningen är produktionsresurser, inte skärmdumpar. Utan de här raderna
# blir en ny favicon-storlek tyst utelämnad ur commiten och 404:ar i drift — de befintliga
# favicon-filerna ligger i repot bara för att någon kom ihåg att köra `git add -f`.
!image/favicon-*.png
!image/apple-touch-icon.png
!image/icon-maskable-*.png
# Arkiv, inte i bruk: ProviaAI-heron från tiden före namnbytet till ExGen. Ingen sida
# refererar den. Den ligger här för att den annars bara fanns på grenen till PR #77 — en
# stängd PR — och hade försvunnit helt när den grenen togs bort. 1,3 MB varumärkeshistoria.
!image/proviaai-hero.png
*.bak
.env
.env.local
.env*.local
*.log
.DS_Store
Thumbs.db
browser.js
screenshot.js
.mcp.json
supabase/.temp/
supabase/backups/*.json

.vercel

# Ruflo / claude-flow runtime
.claude/memory.db
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/data/
.swarm/
ruvector.db
*.rvf.lock

.env*

# eval output (regenerated, not source of truth)
tests/evals/exam-quality/results/

# Testutdata (skärmbilder, rapporter)
.test-out/

# Local git worktrees created by Claude Code sessions. Only this subdirectory —
# the rest of .claude/ (docs, commands, skills, settings) is tracked on purpose.
.claude/worktrees/

# Evalbilder för avläsning av handskrivna lösningar.
# De syntetiska genereras av make-images.mjs och behöver inte versionshanteras.
# Riktiga elevfoton får ALDRIG committas — de innehåller handskrift och ofta
# namn i marginalen, alltså personuppgifter om barn.
tests/evals/solution-ocr/images/
